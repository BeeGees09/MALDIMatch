% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DbLCMS.R
\name{DbLCMS}
\alias{DbLCMS}
\title{Create an LC-MS Database for METASPACE}
\usage{
DbLCMS(lcms, mod_col = NULL, output_path = NULL)
}
\arguments{
\item{lcms}{A data frame containing at minimum the columns \code{Gene},
a modified-sequence column (auto-detected), and \code{Formula}.}

\item{mod_col}{Character. Name of the modified-sequence column. Auto-detected
if \code{NULL} (default), consistent with \code{\link[=ComputeIon]{ComputeIon()}}.}

\item{output_path}{Character path for an optional tab-delimited TSV.
Default \code{NULL} (no file written).}
}
\value{
A data frame with columns \code{id}, \code{name}
(\code{Gene|Modified.Sequence}), and \code{formula}.
}
\description{
Builds a three-column METASPACE-compatible database table from an annotated
LC-MS data frame produced by \code{\link[=ComputeIon]{ComputeIon()}}.
}
\examples{
\dontrun{
lcms_ann <- ComputeIon(read.csv("combined_modified_peptide.csv"))
db <- DbLCMS(lcms_ann, output_path = "Kidney_ECM_LCMS_DB.tsv")
}
}
