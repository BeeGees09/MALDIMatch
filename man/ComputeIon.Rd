% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ComputeIon.R
\name{ComputeIon}
\alias{ComputeIon}
\title{Compute Ion Masses and Isotopic Distributions}
\usage{
ComputeIon(
  lcms,
  mod_col = NULL,
  adducts = c(`[M+H]+` = 1.00728, `[M+Na]+` = 22.98922, `[M+K]+` = 38.96316),
  mod_adjust = .mod_adjust,
  calc_isotopes = TRUE,
  output_path = NULL
)
}
\arguments{
\item{lcms}{A data frame with a modified-peptide-sequence column.}

\item{mod_col}{Character. Column name for modified sequences. Auto-detected
if \code{NULL} (default).}

\item{adducts}{Named numeric vector of adduct mass offsets (Da). Names become
column names in the output. Default: \verb{[M+H]+}, \verb{[M+Na]+}, \verb{[M+K]+}.}

\item{mod_adjust}{Named list of elemental deltas for each modification key.
Extend the built-in list to support additional modifications.}

\item{calc_isotopes}{Logical. If \code{TRUE} (default), computes the full isotopic
distribution (M+0 to M+4) via \code{OrgMassSpecR::IsotopicDistribution}.
Set to \code{FALSE} to skip this — much faster for large datasets when isotope
columns are not needed.}

\item{output_path}{Character path for an optional simplified CSV. Default
\code{NULL} (no file written).}
}
\value{
The \code{lcms} data frame with added columns: one m/z column per adduct,
\code{MH_plus} (alias for \verb{[M+H]+}), \code{Formula}. When \code{calc_isotopes = TRUE},
also adds \code{Max_Peak_mz}, \code{Max_Peak_Label}, and isotope columns
\code{M0_mz}--\code{M4_mz} / \verb{M0_\%}--\verb{M4_\%}.
}
\description{
For each row of an LC-MS data frame, computes the monoisotopic m/z for each
requested adduct, the molecular formula, and (optionally) the isotopic
distribution (M+0 through M+4).
}
\examples{
\dontrun{
lcms <- read.csv("combined_modified_peptide.csv")

# Full calculation (default)
lcms_ann <- ComputeIon(lcms)

# Fast: adduct m/z and formula only, skip isotopes
lcms_ann <- ComputeIon(lcms, calc_isotopes = FALSE)

# Add ammonium adduct
my_adducts <- c("[M+H]+" = 1.00728, "[M+NH4]+" = 18.034)
lcms_ann <- ComputeIon(lcms, adducts = my_adducts)
}
}
